// Home page - Traditional navigation (no route handler)
// This page will trigger full page loads when navigated to

console.log('Home page loaded');

// You can still have page-specific logic here
document.addEventListener('DOMContentLoaded', () => {
    console.log('Home page DOM ready');
});