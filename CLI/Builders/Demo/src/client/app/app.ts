// Webstir Demo App
// This file demonstrates app-level initialization and routing setup

console.log('Webstir Demo App initialized');

// The app will automatically detect and load the router if SPA pages are present
// No manual router setup needed - it's handled by the framework!